# Compiled binaries
immich-go-ui
immich-go-ui.exe
immich-go-ui.exe~

# Go build cache
backend/vendor/

# Angular build output (generated by `make build`)
frontend/dist/
frontend/node_modules/
frontend/.angular/

# Embedded web assets (populated at build time — not tracked)
backend/embedded/web/*
!backend/embedded/web/.gitkeep

# Downloaded immich-go binary (auto-downloaded at runtime)
~/.immich-go-ui/

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
*.suo
*.user
